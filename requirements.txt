# Python dependencies for the RAG system
pandas>=2.0
openpyxl>=3.1              # Excel read/write engine
sentence-transformers>=2.2 # Local embeddings (all-MiniLM-L6-v2)
numpy>=1.24
python-dotenv>=1.0         # .env file support for config
tabulate>=0.9              # Markdown table rendering (pd.to_markdown)
